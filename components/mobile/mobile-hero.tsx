'use client';

/**
 * MobileHero — gyroscope-driven parallax hero.
 *
 * Phase 1: stub.
 * Phase 2:
 *   - Topographic background layers respond to DeviceOrientation (gamma/beta)
 *   - iOS 13+ requires permission request triggered by user gesture
 *   - Tap triggers I.W.R.L → I.R.L animation (CSS-only, no canvas)
 *   - Fallback: scroll-position parallax for non-gyroscope devices
 *   - "Tap to awaken" CTA with baroque filigree border for iOS permission flow
 */

export function MobileHero() {
  return <div />;
}
